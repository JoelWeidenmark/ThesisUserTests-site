import React, {useContext, useState} from 'react'
import ContextInitState from "./ContextInitState";

const ContextProvider = () => {
    const postState = useState[ContextInitState];

    return (
        <div>
            
        </div>
    )
}

export default ContextProvider
